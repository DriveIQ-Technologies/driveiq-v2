/**
 * Waitlist emails that receive a free Premium week on first sign-in
 * (work order task 09). Prefer the Firestore `waitlist` collection in
 * production so this file does not need an app release to change.
 *
 * Lowercase. Zak can paste the cohort here for a TestFlight build.
 */
export const WAITLIST_EMAILS: string[] = [];
